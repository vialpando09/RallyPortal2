@model RallyPortal.Team

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Team</legend>

    <div class="display-label">TeamName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TeamName)
    </div>

    <div class="display-label">DriverName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DriverName)
    </div>

    <div class="display-label">CoDriverName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CoDriverName)
    </div>

    <div class="display-label">TeamImageUrl</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TeamImageUrl)
    </div>

    <div class="display-label">DriverImageUrl</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DriverImageUrl)
    </div>

    <div class="display-label">CoDriverImageUrl</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CoDriverImageUrl)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
